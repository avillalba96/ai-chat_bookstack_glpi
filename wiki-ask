#!/usr/bin/env bash
set -euo pipefail

DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

if [[ -f "$DIR/.env" ]]; then
  # shellcheck disable=SC1091
  set -a
  source "$DIR/.env"
  set +a
fi

exec python3 "$DIR/wiki_ask.py" "$@"

